#include<stdio.h>
#include<stdlib.h>

 int main(int argc, char*argv[]);

  {
   int num=atoi(argv[]);
   argc("receba um n�mero e o divida por 5");
   argv[int resp=(num%5==0, num*4)];
   argv[int resp=(num%5!=0, num*3)];
  }

  pritf("resp=%d\n", resp);

  return 0;

